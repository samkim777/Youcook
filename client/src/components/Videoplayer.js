import axios from "axios"

export const VideoPlayer = () => {
    return <div> </div>
}

const getVideoId = async () => {
    try {
        const videos = ['a','b'];
        // response = await axios.get("http://localhost:3001/videoInfo")
    } catch (err) {
        console.error({error: err})
    }
}